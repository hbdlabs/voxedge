apiVersion: v1
kind: Service
metadata:
  name: voxedge
  namespace: voxedge
  labels:
    app.kubernetes.io/part-of: voxedge
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: voxedge
