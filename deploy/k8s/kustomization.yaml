apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - pvc.yaml
  - service.yaml
  - ingress.yaml
  # Uncomment ONE deployment profile:
  - deployment-gemma.yaml
  # - deployment-aya.yaml
